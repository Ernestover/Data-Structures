#include <stdio.h> // Include standard input/output library

int main()
{
    printf("Hello from C on VS Code on Mac!\n"); // Print a message to the console
    return 0;                                    // Indicate successful execution
}